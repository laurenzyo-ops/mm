import ChunkSceneWrapper from "@/components/ChunkSceneWrapper";

export default function Home() {
  return (
    <main style={{ display: "flex", justifyContent: "center", padding: 20 }}>
      <ChunkSceneWrapper />
    </main>
  );
}